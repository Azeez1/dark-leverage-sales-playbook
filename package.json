{
  "name": "dark-leverage-sales-playbook",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --outfile=public/bundle.js --minify --jsx=automatic --loader:.js=jsx",
    "start": "npm run build && npx serve public"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.6",
    "serve": "^14.2.4"
  }
}
